
public class  Object{

		public Object(){

		// logic to
		// create instances
		// memory allocation in heap
		// creating copy of instance vars
		// generating hashcode ..

		}


	String toString();  // hashcode in hexadecimal format with classname

	boolean equals();  // it compares to object references or hashcodes

		finalize();
		getClass();
		wait();
		notify();
		notifyAll(;
		clone();
		

}